apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaayaka-re
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaayaka-re
  template:
    metadata:
      labels:
        app: kaayaka-re
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kaayaka-re
          image: naveensasalu/kayaka-re:1e252e98a6fb650d818c6518573d691a67835450
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
