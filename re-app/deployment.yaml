apiVersion: apps/v1
kind: Deployment
metadata:
  name: kaayaka-re
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kaayaka-re
  template:
    metadata:
      labels:
        app: kaayaka-re
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kaayaka-re
          image: naveensasalu/kayaka-re:cb798d3c90b5381fba7a277e42a335209b3f6830
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
